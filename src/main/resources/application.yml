spring:
  datasource:
    url: jdbc:mysql://localhost:3308/spring_lab
    username: admin
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      show_sql: true
      format_sql: true
  flyway:
    enabled: true
  output:
    ansi:
      enabled: always